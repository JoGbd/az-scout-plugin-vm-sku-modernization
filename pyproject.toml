[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "az-scout-plugin-vm-migration-scope"
dynamic = ["version"]
description = "Legacy SKU VM inventory and migration-scope dashboard for Azure v6/v7 planning"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "az-scout",
    "fastapi",
]

[project.urls]
Homepage = "https://github.com/JoGbd/az-scout-plugin-vm-migration-scope"
Repository = "https://github.com/JoGbd/az-scout-plugin-vm-migration-scope"
Issues = "https://github.com/JoGbd/az-scout-plugin-vm-migration-scope/issues"

[dependency-groups]
dev = [
    "pytest>=9.0",
    "pytest-cov>=6.0",
    "ruff>=0.11",
    "mypy>=1.15",
    "httpx>=0.28",
]

[project.entry-points."az_scout.plugins"]
vm_migration_scope = "az_scout_vm_migration_scope:plugin"

[tool.hatch.build.targets.wheel]
packages = ["src/az_scout_vm_migration_scope"]

[tool.hatch.version]
source = "vcs"
raw-options.version_scheme = "guess-next-dev"
raw-options.local_scheme = "no-local-version"
raw-options.fallback_version = "0.1.0"

[tool.hatch.build.hooks.vcs]
version-file = "src/az_scout_vm_migration_scope/_version.py"

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["src/az_scout_vm_migration_scope/_version.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
